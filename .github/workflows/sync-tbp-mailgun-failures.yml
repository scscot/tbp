name: Sync TBP Mailgun Failures

on:
  schedule:
    # Daily at 5:30am PT (13:30 UTC) - offset from PreIntake sync
    - cron: '30 13 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no changes)'
        required: false
        default: 'false'
        type: boolean

jobs:
  sync-failures:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scripts

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install firebase-admin axios
        working-directory: scripts

      - name: Create secrets directory
        run: mkdir -p ../secrets

      - name: Write service account key
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > ../secrets/serviceAccountKey.json

      - name: Write Mailgun config
        run: |
          cat << 'EOF' > ../secrets/mailgun_config.json
          {
            "tbp_api_key": "${{ secrets.TBP_MAILGUN_API_KEY }}",
            "tbp_domain": "news.teambuildpro.com",
            "preintake_api_key": "${{ secrets.MAILGUN_API_KEY }}",
            "preintake_domain": "law.preintake.ai"
          }
          EOF

      - name: Sync TBP Mailgun failures to Firestore
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            node sync-tbp-mailgun-failures.js --dry-run
          else
            node sync-tbp-mailgun-failures.js
          fi
