name: FSR ID Harvester

on:
  schedule:
    # Run 12x daily (every 2 hours) for aggressive ID harvesting
    - cron: '0 0 * * *'    # 4pm PT
    - cron: '0 2 * * *'    # 6pm PT
    - cron: '0 4 * * *'    # 8pm PT
    - cron: '0 6 * * *'    # 10pm PT
    - cron: '0 8 * * *'    # 12am PT
    - cron: '0 10 * * *'   # 2am PT
    - cron: '0 12 * * *'   # 4am PT
    - cron: '0 14 * * *'   # 6am PT
    - cron: '0 16 * * *'   # 8am PT
    - cron: '0 18 * * *'   # 10am PT
    - cron: '0 20 * * *'   # 12pm PT
    - cron: '0 22 * * *'   # 2pm PT

  workflow_dispatch:
    inputs:
      max_pages:
        description: 'Maximum pages to process'
        default: '50'
      state:
        description: 'Start from specific state (optional)'
        default: ''
      dry_run:
        description: 'Dry run mode (no Firestore writes)'
        default: false
        type: boolean

env:
  NODE_VERSION: '20'

jobs:
  harvest:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'scripts/package.json'

      - name: Install dependencies
        run: cd scripts && npm ci

      - name: Install Chrome dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 libatk-bridge2.0-0 libdrm2 \
            libxkbcommon0 libgbm1 libasound2t64 libxshmfence1

      - name: Setup Firebase credentials
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          mkdir -p secrets
          echo "$FIREBASE_SERVICE_ACCOUNT" > secrets/serviceAccountKey.json

      - name: Run harvester (scheduled)
        if: github.event_name == 'schedule'
        run: |
          # Harvest user IDs from state+company pages (25 states × 272 companies = 6,800 combinations)
          # No CAPTCHA needed - runs fast, 50 pages per run
          node scripts/fsr-id-harvester.js --harvest --max-pages=50

      - name: Run harvester (manual)
        if: github.event_name == 'workflow_dispatch'
        env:
          MAX_PAGES: ${{ github.event.inputs.max_pages }}
          STATE: ${{ github.event.inputs.state }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          ARGS="--harvest --max-pages=$MAX_PAGES"
          if [ -n "$STATE" ]; then
            ARGS="$ARGS --state=$STATE"
          fi
          if [ "$DRY_RUN" = "true" ]; then
            ARGS="$ARGS --dry-run"
          fi
          node scripts/fsr-id-harvester.js $ARGS

      - name: Show stats
        if: always()
        run: node scripts/fsr-id-harvester.js --stats || true

      - name: Cleanup
        if: always()
        run: rm -rf secrets/
