name: Scentsy Consultant Scraper

on:
  schedule:
    # Run every 6 hours: 6am, 12pm, 6pm, 12am PT (UTC: 14, 20, 02, 08)
    - cron: '0 14,20,2,8 * * *'

  # Allow manual trigger for testing
  workflow_dispatch:
    inputs:
      max_zipcodes:
        description: 'Maximum zipcodes to scrape per run'
        required: false
        default: '50'
        type: string
      country:
        description: 'Country filter (US, CA, GB, DE, NL, AU)'
        required: false
        type: string
      dry_run:
        description: 'Dry run (preview only, no Firestore writes)'
        required: false
        default: false
        type: boolean
      reset:
        description: 'Reset all scraped flags before running'
        required: false
        default: false
        type: boolean

env:
  NODE_VERSION: '20'

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'scripts/package.json'

      - name: Install dependencies
        run: |
          cd scripts && npm ci && cd ..
          echo "Dependencies installed"

      - name: Install Puppeteer Chrome dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 \
            libatk-bridge2.0-0 \
            libdrm2 \
            libxkbcommon0 \
            libgbm1 \
            libasound2t64 \
            libxshmfence1
          echo "Chrome dependencies installed"

      - name: Setup Firebase credentials
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          mkdir -p secrets
          echo "$FIREBASE_SERVICE_ACCOUNT" > secrets/serviceAccountKey.json
          echo "Firebase credentials configured"

      - name: Reset scraped flags (if requested)
        if: ${{ github.event.inputs.reset == 'true' }}
        run: |
          echo "Resetting all scraped flags..."
          node scripts/scentsy-scraper.js --reset
          echo "Reset complete"

      - name: Show pre-run stats
        run: |
          echo "Pre-run stats:"
          node scripts/scentsy-scraper.js --stats || true

      - name: Run Scentsy Scraper
        env:
          MAX_ZIPCODES: ${{ github.event.inputs.max_zipcodes || '50' }}
          COUNTRY: ${{ github.event.inputs.country }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          echo "Starting Scentsy Consultant Scraper..."
          echo "Max zipcodes per run: $MAX_ZIPCODES"

          ARGS="--scrape --max=$MAX_ZIPCODES"

          if [ -n "$COUNTRY" ]; then
            echo "Country filter: $COUNTRY"
            ARGS="$ARGS --country=$COUNTRY"
          fi

          if [ "$DRY_RUN" = "true" ]; then
            echo "Mode: DRY RUN"
            ARGS="$ARGS --dry-run"
          else
            echo "Mode: LIVE"
          fi

          node scripts/scentsy-scraper.js $ARGS

      - name: Show final stats
        if: always()
        run: |
          echo "Final stats:"
          node scripts/scentsy-scraper.js --stats || true

      - name: Cleanup
        if: always()
        run: |
          rm -rf secrets/
          echo "Cleanup complete"
